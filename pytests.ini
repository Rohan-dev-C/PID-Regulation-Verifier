[pytest]
# Add THIS checkout’s root dir (.) to PYTHONPATH → `import src.*` works.
pythonpath = .

# Only collect tests from this repo’s tests/ folder.
testpaths   = tests

addopts     = -ra    # nice summary at end
